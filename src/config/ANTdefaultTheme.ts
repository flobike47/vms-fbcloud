import {theme} from "antd";

const defaultTheme = {
    algorithm: theme.compactAlgorithm,
        token: {
        colorPrimary: '#ec8c3f',
            borderRadius: 5,
            colorBgContainer: '#919190',
    },
};

export { defaultTheme };